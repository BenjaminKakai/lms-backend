# Server port
server.port=8081

# Eureka Client Configuration (Connecting to Eureka Server)
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/lmsbackend
spring.datasource.username=postgres
spring.datasource.password=co37x74bob

# Hibernate Properties
spring.jpa.show-sql=true

# Logging Levels
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework=DEBUG

# Spring Security (if you want basic auth)
spring.security.user.name=Benjamin
spring.security.user.password=co37x74bob

spring.main.allow-circular-references=true

